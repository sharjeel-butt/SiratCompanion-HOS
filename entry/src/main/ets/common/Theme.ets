export class Theme {
  // Primary brand colors
  static readonly primaryGreen = '#1B4D3E';
  static readonly accentGold = '#C9A96E';
  static readonly accentGoldLight = '#E8D5A3';

  // Text colors
  static readonly textWhite = '#FFFFFF';
  static readonly textLight = '#F5F5F5';
  static readonly textMuted = '#A8B5A3';
  static readonly textDark = '#1A1A1A';

  // Card backgrounds
  static readonly cardDark = '#1E3A2F';
  static readonly cardLight = '#2A5A48';
  static readonly cardMuted = '#2D4A3A';

  // Gradients
  static readonly primaryGradient = ['#1B4D3E', '#2A6B50'];
  static readonly goldGradient = ['#C9A96E', '#B8954E'];

  // Spacing
  static readonly paddingSmall = 8;
  static readonly paddingMedium = 16;
  static readonly paddingLarge = 24;
  static readonly radiusSmall = 8;
  static readonly radiusMedium = 16;
  static readonly radiusLarge = 24;
}